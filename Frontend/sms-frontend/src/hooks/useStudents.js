// I BUILD THIS — custom hook that fetches all students and manages loading/error state
// You use this hook in StudentsPage.jsx like:
//   const { students, loading, error } = useStudents();
// Do not modify. I will fill this in when we reach the student list step.

export function useStudents() {
    return { students: [], loading: false, error: null }; // placeholder
}
