// YOUR TASK — Page 2 (build AFTER LoginPage is done)
// Display the list of all students using the useStudents hook I provide.
//
// HOW TO USE THE HOOK:
//   const { students, loading, error } = useStudents();
//
// JSX LOGIC:
//   if (loading) → show "Loading..." text
//   if (error)   → show error message in red
//   otherwise    → map over students array, for each student render a <Card> showing:
//                  firstName + lastName as heading, email, status
//
// WHEN DONE: tell me "I finished StudentsPage.jsx" and paste your code.

export default function StudentsPage() {
    // TODO: call useStudents hook and return the JSX
}
