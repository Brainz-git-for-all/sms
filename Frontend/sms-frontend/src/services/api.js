// I BUILD THIS — the central Axios instance used by all service files
// Every service imports from here. It:
//   1. Sets the base URL to http://localhost:8080 so services only write /api/...
//   2. Automatically attaches the JWT token from localStorage to every request
// Do not modify. I will fill this in when we start the frontend API step.

const api = null; // placeholder — I replace this

export default api;
