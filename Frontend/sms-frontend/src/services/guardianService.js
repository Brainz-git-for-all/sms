// I BUILD THIS — API calls for the guardian role
// One function:
//   getChildrenGrades() → GET /api/guardians/my-children-grades
//   Returns array of { student: { firstName, lastName, ... }, grades: [...] }

import api from './api';

// I build this when we reach the guardian page step
