// SHARED FILE — I write login(), you write register()
//
// BOTH functions use the api instance from api.js like this:
//   import api from './api';
//   export const login = (username, password) => api.post('/api/auth/login', { username, password });
//
// YOUR TASK — write register():
//   function name: register
//   parameters:    username (string), password (string), role (string)
//   what it does:  POST to /api/auth/register with body { username, password, role }
//   return:        the axios call (same as login — just api.post with different url and body)
//
// I will write login() first so you see the exact pattern, then you add register() below it.

import api from './api';

// I write login() here when we reach this step

// You write register() below it
