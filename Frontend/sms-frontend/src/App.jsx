// I BUILD THIS — routing will be added here when we set up React Router + AuthContext
// For now it renders LoginPage so you can see your components immediately

import LoginPage from './pages/LoginPage';

function App() {
    return <LoginPage />;
}

export default App;
